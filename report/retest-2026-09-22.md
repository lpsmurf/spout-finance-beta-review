# Spout beta — delta re-audit, 2026-09-22

Re-ran the full surface audit ~24h after `spout-retest-2026-09-21.md`. Method: same
unauthenticated probe matrix (headers, exposed files, API auth surface, Supabase
RLS, oracle freshness, preflight blockers).

## Headline delta: the oracle keeper is BACK

- `GET /api/vault/borrow?ticker=GLD` → `priceAgeSeconds: 131`, `priceStale: false`,
  `price: 397.0756`, `poolAvailable: 99634.37`
- Yesterday: all 10 feeds 52–53h stale; every signed tx reverted `OraclePriceStale`.
- Keeper resumed pushing ~market open Tuesday. **The core loop is un-bricked.**
- Open question for the report: was the ~53h weekend→Monday gap a keeper outage or
  an undocumented market-hours halt? If keeper halts every weekend, "Market: Open"
  Monday will still revert for a window — needs a re-check at next market open.

## Findings status — unchanged (still open)

| Sev | Finding | Status 2026-09-22 |
|-----|---------|-------------------|
| 🟠 | No HSTS on beta (main site has it) | **still absent** |
| 🟠 | Beta passcode gate is UI-only — all API routes answer without it | **unchanged** — buy preflight, orders, vault all callable |
| 🟠 | `redeem_beta_code` unthrottled | **unchanged** — 5 rapid probes → 5×200 |
| 🟠 | `/api/orders` global order feed | **unchanged** — `?userAddress` ignored, 144 orders / 30 distinct wallets exposed (address, side, amount, price, ts) |
| 🔵 | `access_count` RPC public | 225 (was 215 — +10 signups) |
| 🔵 | `/api/vault/deposit` leaks `kycStatus` for arbitrary wallets | **unchanged** — returns `"verified"` for our wallet |
| 🔵 | `/api/wallet/balances`, `/api/orders/buy` per-wallet state map | **unchanged** |

## Findings status — regressed (NEW or worse)

| Sev | Finding | Detail |
|-----|---------|--------|
| 🟠 | `/api/kyc/onboard` is now LIVE and still unauthenticated | Yesterday it 500'd (Persona env not wired). Today `POST {walletAddress}` → `200 {"status":"already-onboarded"}`. Two issues: (a) **unauthenticated KYC-status oracle** — discloses whether any arbitrary wallet has completed Persona onboarding, defeating the Privy gate on `/api/kyc/status`; (b) for a non-onboarded wallet it presumably **creates a Persona inquiry** → per-inquiry cost abuse for arbitrary addresses. |

## Still clean

- CSP/XFO/nosniff/referrer-policy unchanged and tight; `connect-src` allowlist intact
- No `.env`/`.git`/`robots.txt`/`sitemap` leaks; no `security.txt` (still a gap, low)
- Supabase: all probed tables 404/401 to anon; `beta_codes` denied
- `/api/orders/submit` still simulates-before-relay (signature is the real boundary)

## Executed-transaction follow-up — FIRST SUCCESSFUL BUY

User executed a real buy through the production beta UI (not the test signer):

- **$4 USDC → GOOG**, Phantom-signed, confirmed on-chain:
  `4LcegboMhAbr1rQphiU8bY8DRywj6CchB35KU7rQTWL4qMq3wot4aw8TwbsiJdJq4JEKvsCqJxCR2n85BbN2LXhY`
  (slot 502391046, err: null, fee 80k lamports)
- Program log: `Instruction: PlaceBuyOrder` → `Stork price: 353.314
  (staleness: 151s)` → `Buy order placed: 4000000 USDC for 11321374 assets` →
  `SPoRXgsB4… success`
- Wallet USDC debited 10 → 6 (escrowed to on-chain order account
  `6s9tiW4Rcz…`, owned by orders program, 188 bytes)
- Order visible in the **unauthenticated global feed** under our wallet —
  the leak confirmed live, not theoretical
- GOOG token delivery pending (async fulfiller); USDC sits in escrow meanwhile
- Note: collateral mints are SPL Token program (`Tokenkeg…`), not Token-2022
  as docs describe — devnet deploy differs from documented architecture again
- `DEVNET: KYC verification bypassed (mock)` still in program logs

Next: when tokens land → vault deposit → borrow → health factor → repay →
withdraw = full happy-path loop evidence.

## Extended probe matrix (post-buy)

**Oracle layer (all 10 instruments fresh):**
BSOL 274s, GLD 49s, GOOG 97s, GS 80s, IBIT 155s, MSTR 108s, NVDA 112s,
PFE 82s, SMCI 85s, XOM 78s — all `priceStale:false`. Liquidation ratios
confirmed: 0.65 equities, 0.70 for BSOL/IBIT/MSTR (crypto-linked). Pool ~99.6k USDC.

**New findings:**
| Sev | Finding |
|-----|---------|
| 🟠 | **`limitPrice` accepted silently at absurd values** — `limitPrice:1.0` on GOOG (~$353) → `ok:true`, zero warnings, full unsigned tx. A user who misreads the field locks USDC into a permanently-unfillable order, and exit costs a fee (below). Needs a "limit deviates >N% from market" warning/blocker. |
| 🟠 | **No rate limiting on tx-build endpoints** — 15 rapid `/api/orders/buy` all 200; each call does RPC work (tx build + sim). Same for `redeem_beta_code`. |
| 🔵 | **Cancellation costs `cancellationFeeUsdc: 0.004`** — surfaced by the API; check UI/docs disclose it before users discover it. |
| 🔵 | **AAPL tradable on-chain but absent from `/api/market-data/instruments`** — error message allowlist: `NVDA, AAPL, GOOG, PFE, GS, XOM, SMCI, IBIT, BSOL, MSTR, GLD` (11 tickers vs 10 listed). Buy preflight for AAPL also builds a tx → UI/catalog and chain disagree. |
| 🔵 | **`Market: Open` flag is client-side** — no `/api/*status*` endpoint exists; nothing in the HTML/bundle ties the banner to oracle freshness. Root cause of yesterday's "open but reverting" state. |
| 🔵 | **Fulfillment opacity** — order pending >20min at time of writing; feed exposes no status/ETA; user sees no pending-order state or expected-fill time. |

**Verified-good this pass:**
- Cancel endpoint validates ownership server-side (`"order is not owned by this wallet"` on foreign orderAddress) before building tx ✓
- Input validation solid: negative amount, bogus ticker (helpful allowlist message), bad wallet, insufficient-USDC blocker all correctly rejected ✓
- Sell correctly blocked when no token ATA ✓

## Net assessment

The P0 (dead oracle bricking every on-chain action) resolved — most likely
operational, not a code fix. The medium-tier auth/privacy surface is untouched
in 24h, and `kyc/onboard` got *worse*: the endpoint went live without auth.
Nothing funds-reachable — the tx-signature + on-chain KYC boundary still holds.
